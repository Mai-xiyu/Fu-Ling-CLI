# 符灵快速开始指南

## 🚀 5分钟上手

### 1. 安装
```bash
# 克隆并安装
git clone https://github.com/mai-xiyu/Fu-Ling-CLI.git
cd Fu-Ling-CLI
pip install -e .

# 或直接安装（PyPI发布后）
# pip install fuling
```

### 2. 首次运行
```bash
# 初始化符灵
fl init

# 测试基础功能
fl explain "ls -la"
fl wisdom
fl power
```

### 3. 选择AI提供商
```bash
# 配置AI提供商（交互式）
fl config

# 选项：
# - Moonshot (Kimi) - 推荐，中文优化
# - OpenAI - 国际标准
# - 本地模式 - 无需API密钥
```

## 📋 核心命令速查表

### 基础命令
```bash
# 解释shell命令
fl explain "docker run"
fl explain "git commit -m" --context "版本控制"

# 生成代码
fl generate "python快速排序" -l python
fl generate "react按钮组件" -l javascript -o Button.js

# 与AI对话
fl chat

# 获取帮助和建议
fl wisdom
fl fortune
```

### 系统管理
```bash
# 检查系统状态
fl power

# 配置设置
fl config

# 更新配置
fl init --theme modern  # 切换主题
```

## 🎯 常见使用场景

### 开发者使用
```bash
# 解释复杂命令
fl explain "kubectl apply -f deployment.yaml"

# 生成样板代码
fl generate "fastapi用户端点" -l python -o users.py

# 获取命令建议
fl fortune
```

### 系统管理员使用
```bash
# 监控系统
fl power

# 学习新命令
fl explain "netstat -tulpn"
fl explain "journalctl -f"

# 获取故障排除提示
fl wisdom
```

### 学习使用
```bash
# 交互式学习
fl chat
# 提问："'grep -r pattern .' 是做什么的？"
# 提问："如何查找大文件？"

# 每日练习
fl fortune  # 获取随机命令学习
```

## 🔧 配置指南

### 基础配置
```bash
# 使用特定主题初始化
fl init --theme dark

# 配置AI提供商
fl config
# 跟随交互式提示
```

### 环境变量
```bash
# 设置API密钥（可选）
export MOONSHOT_API_KEY='your_key'      # Moonshot使用
export OPENAI_API_KEY='your_key'        # OpenAI使用

# 主题偏好（可选）
export FULING_THEME='modern'
export FULING_LANGUAGE='zh'
```

### 配置文件
- **主配置**: `~/.config/fuling/config.yaml`
- **缓存**: `~/.cache/fuling/`
- **日志**: `~/.local/share/fuling/logs/`

## 🎨 个性化定制

### 主题
```bash
# 可用主题
fl init --theme ancient   # 古风主题（默认）
fl init --theme modern    # 现代简约
fl init --theme dark      # 暗黑模式
fl init --theme light     # 明亮模式
```

### 语言
```bash
# 在config.yaml中设置语言
language: "zh"  # 或 "en" 英文
```

## 🚨 故障排除

### 常见问题

**问题**: `fl` 命令找不到
```bash
# 重新安装
pip install -e .

# 检查安装
which fl
```

**问题**: API错误
```bash
# 检查API密钥
echo $MOONSHOT_API_KEY

# 测试连接
fl power

# 切换到本地模式
fl config  # 选择"本地模式"
```

**问题**: 响应缓慢
```bash
# 检查网络
fl power

# 临时使用本地模式
# 本地模式可离线工作
```

## 📚 下一步

### 掌握基础功能
1. ✅ 安装和初始化
2. ✅ 学习核心命令
3. ✅ 配置AI提供商
4. ✅ 尝试不同主题

### 探索高级功能
1. 使用 `fl generate` 生成代码
2. 尝试 `fl chat` 交互式帮助
3. 创建自定义配置
4. 加入社区

### 获取帮助
- 运行 `fl wisdom` 获取提示
- 运行 `fl --help` 查看命令参考
- 查看 `docs/` 获取详细文档
- 加入GitHub讨论

---

**你已准备好使用符灵！从 `fl wisdom` 开始获取每日提示。** 🚀